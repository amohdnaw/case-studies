# Queued studies (picked 2026-09-25)

Each gets its own grill (question, data safety, outcome contract) before any writing. Not started.

1. **Preregistered trade test.** Do trend, failed-break and volatility-buffer variants beat control
   after cost, out of sample? Evidence: `~/trading-signals/experiments/interim-readout-2026-09-23.md`.
   Blocked until the preregistration closes on 2026-10-01. Public market data, safe. ~3-4 h. Pairs
   with 01/05 under Research honesty.
2. **DONE 2026-09-25 as Study 11 (docs/machine-identity.html), angle: does the name-token holdout transfer to blank charts.** **Machine identity at scale.** What counts as "a machine" when SPC, PCS config and stop feeds
   disagree? Evidence: `~/html-dashboard/spc/reports/build/blf_config.py`,
   `chart_register.py`, NOW.md BLF section. Needs a from-scratch synthetic replica; never floor data,
   never spc-api :3005. ~8-12 h. Likely a Measurement systems or new "Data integrity" section.
3. **DONE 2026-09-25 as Study 10 (docs/cv-gates.html).** **Gates that pass vacuously.** The CV-pipeline audit that passed all 18 packs while a contact row
   wrapped (a rejoined-line extraction cannot see a wrap). Evidence: `~/career-ops/` ats_check.py
   check 6, pack_audit `head_raw()`. Own documents, safe. ~4-5 h.

4. **The export unit trap (candidate, Data integrity).** An export whose row is not the unit you
   count, off by more than 25 times. Ratios only if written. Not grilled yet.
5. **Two disputed master lists (candidate, Data integrity).** Two machine lists that each claim to
   be the master and disagree. Needs a synthetic replica. Not grilled yet.
